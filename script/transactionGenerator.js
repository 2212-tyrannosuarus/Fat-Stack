const generateTransactions = (user, vendorArr, accountIdArr, startingDate) => {
  //takes an array of vendors
  //subcategory will be attached to vendors
  //takes
  //run two transactions per day
  //monthly bills
  //monthly income

  //loop over each date
  let currDate = startingDate;
  for (let i = 0; i < 365; i++) {}
};
